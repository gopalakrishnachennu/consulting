Django>=6.0.6,<7.0
psycopg2-binary>=2.9.12
dj-database-url>=2.1
redis>=5.0
celery>=5.3
django-celery-beat>=2.9.0
django-celery-results>=2.6.0
django-htmx>=1.27.0
django-tailwind>=4.4.2
django-browser-reload>=1.21.0
django-widget-tweaks>=1.5.1
python-decouple>=3.8
Pillow>=10.2
python-docx>=1.1
xhtml2pdf>=0.2.11
openai>=1.10
beautifulsoup4>=4.12       # HTML scraping for harvest engine
lxml>=5.0                  # Faster HTML parser for BeautifulSoup
whitenoise>=6.6
gunicorn>=21.2
django-extensions>=4.1
ipython>=9.14.1
pytest-django>=4.7
coverage>=7.4
cryptography>=42.0
sentry-sdk>=2.0
country-converter>=1.3.2          # ISO country name/code normalisation
pyyaml>=6.0                     # dept_rules.yaml / dept_anchors.yaml loading
# sentence-transformers is optional — install from requirements-ml.txt on GPU/dev boxes
# The classifier falls back to LLM (Tier 4) automatically if not installed

# ── MCP (Model Context Protocol) server ───────────────────────────────────────
mcp>=1.0                        # Anthropic MCP SDK — exposes GoCareers tools to Claude / AI clients
